# Changesets

This folder contains changeset files. Each file describes a change to one or more packages in this monorepo.

## Adding a changeset

Run `pnpm changeset` and follow the prompts.

## More information

For more information about changesets, visit [the changesets documentation](https://github.com/changesets/changesets).
